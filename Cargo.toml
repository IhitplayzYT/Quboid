[package]
name = "Quboid"
description = ""
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
license-file = "LICENSE"

[dependencies]
uuid = { version = "1" , features = ["v4","v7","serde"] }
mysql = { version = "27", features = ["chrono"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.10.2"
regex = "1"